name: Génération et déploiement de la documentation
"on":
  pull_request:
    types: [closed]
    branches: [ main ]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  deploy-docs:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Placer dans le dépôt
        uses: actions/checkout@v4
      - name: Installation de Java 21 (corretto)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'corretto'
      - name: Génération de la Javadoc
        run: mvn -B javadoc:javadoc
      - name: Repérage du dossier généré
        id: javadoc-dir
        run: echo "path=$(find target -type d -name apidocs | head -n 1)" >> "$GITHUB_OUTPUT"
      - name: Déploiement sur GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ${{ steps.javadoc-dir.outputs.path }}
